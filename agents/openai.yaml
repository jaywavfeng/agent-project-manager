interface:
  display_name: "Agent Project Manager"
  short_description: "Project memory, clean workspaces, optional delegation"
  default_prompt: "$apm Resume from repository state and project memory, work standalone unless delegation is clearly cheaper, verify results, and keep the active context and workspace small."
policy:
  allow_implicit_invocation: false
