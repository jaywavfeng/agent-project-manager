interface:
  display_name: "Tiered Agent Orchestrator"
  short_description: "Cost-aware execution, handoffs and workspace housekeeping"
  default_prompt: "$tao Resume from repository state, choose direct execution or reusable independent Workers by total cost, verify results, and keep coordination and the active workspace small."
policy:
  allow_implicit_invocation: false
